import 'package:flutter/material.dart';

class DnaVisual extends StatelessWidget {
  const DnaVisual({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 120,
      child: Center(child: Icon(Icons.biotech, size: 64)),
    );
  }
}
